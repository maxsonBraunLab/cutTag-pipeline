__default__:
    mem: 16G
    partition: "exacloud"
    time: "03:00:00"
    nodes: 1
    cpus-per-task: 8
    job-name: "{rule}"
    error: "jobs/{rule}/{rule}_%j.err"
    out: "jobs/{rule}/{rule}_%j.out"
callpeaks:
    mem: 64G
diffbind:
    cpus-per-task: 4
    mem: 40G
