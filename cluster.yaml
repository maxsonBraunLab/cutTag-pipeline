__default__:
    mem: 8G
    partition: "batch"
    time: "03:00:00"
    nodes: 1
    cpus-per-task: 4
    job-name: "{rule}"
    error: "jobs/{rule}/{rule}_%j.err"
    out: "jobs/{rule}/{rule}_%j.out"
bowtie2:
    cpus-per-task: 8
callpeaks:
    mem: 64G
tracks:
    cpus-per-task: 8
merge_bw:
    mem: 16G
diffbind:
    mem: 40G
